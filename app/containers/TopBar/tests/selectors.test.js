// import { fromJS } from 'immutable';
// import { makeSelectTopBarDomain } from '../selectors';

// const selector = makeSelectTopBarDomain();

describe('makeSelectTopBarDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
