// import { fromJS } from 'immutable';
// import { makeSelectDashViewDomain } from '../selectors';

// const selector = makeSelectDashViewDomain();

describe('makeSelectDashViewDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
